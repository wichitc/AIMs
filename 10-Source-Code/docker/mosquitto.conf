# Minimal dev/local MQTT broker config for IoT sensor ingestion (FR-24).
# Production deployments must enable authentication (password_file / TLS) — this
# anonymous-allowed config is for local development only.

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/
log_dest stdout
